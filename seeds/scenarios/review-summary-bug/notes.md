# Bugfix Task

A reviewer relies on the summary panel to decide what is ready for review.
Recently, a session can be ended successfully, but the reviewer summary still looks wrong.

Your task:
- trace the session end flow
- find the root cause
- fix it
- verify the fix

Helpful command:
- `node tests/reviewer-summary.test.js`
